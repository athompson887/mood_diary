/// Feature flags for controlling app features
class FeatureFlags {
  /// Show debug panel in More screen (set to false for production)
  static const bool showDebugPanel = true;

  /// Enable premium features (for future use)
  static const bool enablePremium = false;
}
